import requests
import os
import re
from concurrent.futures import ThreadPoolExecutor, as_completed
from threading import Lock

class Generator:
    # ... (functions for generating voice responses, sending to Eleven Labs API, etc.)
